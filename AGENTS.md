# Project Instructions

- When completing a coding task, review the diff, run the relevant checks, and commit the finished work before reporting completion unless the user asks not to commit.
- Bump the project version for every committed change, including updates to `package.json` and `package-lock.json`.
- When launching or restarting the Work in the Sun server at `F:\projects\work-in-the-sun`, also make sure its MCP server is available: build the backend so `F:\projects\work-in-the-sun\dist\server\mcp-server.js` exists, verify Codex MCP config points at that built entrypoint, and mention if a new Codex session or reload is needed for MCP tool discovery.
